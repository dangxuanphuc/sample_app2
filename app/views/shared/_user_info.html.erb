<%= link_to gravatar_for(current_user, size: Settings.gravatar_size), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to "View my profile", current_user %></span>
<span id="count_micropost"><%= pluralize(current_user.microposts.count, "micropost") %></span>
